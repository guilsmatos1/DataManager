[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "trademachine-backtestengine-broker"
version = "0.1.0"
description = "Broker adapters and MT5-like models for BacktestEngine"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
include = ["trademachine*"]
