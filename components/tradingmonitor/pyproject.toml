[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "trademachine-tradingmonitor"
version = "0.1.0"
description = "trademachine-tradingmonitor brick"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
include = ["trademachine*"]
