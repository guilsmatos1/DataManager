[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "trademachine-backtestengine"
version = "0.1.0"
description = "trademachine-backtestengine brick"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
include = ["trademachine*"]
