[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "trademachine-datamanager-api"
version = "0.1.0"
description = "trademachine-datamanager-api brick"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
include = ["trademachine*"]
