# Integration tests — require external services (OpenBB/yfinance, Dukascopy, network)
# Run explicitly: uv run pytest tests/integration/
# These are NOT executed by the default `uv run pytest` command.
