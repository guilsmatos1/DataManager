# BacktestEngine Project

## Overview
Backtesting engine project. Currently a stub/early-stage project.

## Components
- `trademachine.backtestengine` (component)
- `trademachine.backtest_engine_cli` (base)

## Commands
```bash
# Run tests
uv run pytest components/backtestengine/test/ -v
```
